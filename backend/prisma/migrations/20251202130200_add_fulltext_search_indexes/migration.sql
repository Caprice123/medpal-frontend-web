-- Migration already applied directly to database
-- This migration file created to sync migration history

-- Add fulltext search indexes (if not already exist)
-- CREATE INDEX IF NOT EXISTS ... (indexes were already created)
